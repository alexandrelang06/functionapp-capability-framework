{
  "routes": [
    {
      "route": "/api/{*rest}",
      "rewrite": "https://it-framework.bearingpoint.com/{rest}"
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*"]
  }
}
